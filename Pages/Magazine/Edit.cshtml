@page
@model Farkas_Szabolcs_ProiectExamen.Pages.Magazine.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Magazin</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Magazin.ID" />
            <div class="form-group">
                <label asp-for="Magazin.NumeMagazin" class="control-label"></label>
                <input asp-for="Magazin.NumeMagazin" class="form-control" />
                <span asp-validation-for="Magazin.NumeMagazin" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
