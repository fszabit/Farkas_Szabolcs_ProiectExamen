@page
@model Farkas_Szabolcs_ProiectExamen.Pages.Rezervari.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Rezervare[0].Cumparator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rezervare[0].Produs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rezervare[0].Datarezervarii)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Rezervare) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cumparator.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Produs.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datarezervarii)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
